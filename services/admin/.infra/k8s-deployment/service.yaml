apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: app
spec:
  selector:
    app: go-fin-microservice
    name: admin-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
